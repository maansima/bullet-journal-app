
#endpoint: ${env:PRISMA_ENDPOINT}
endpoint: https://eu1.prisma.sh/legina-chen/legina/dev

datamodel: datamodel.graphql

disableAuth: true

hooks:
  post-deploy:
    - graphql get-schema -p database
    - graphql prepare
    - graphql codegen